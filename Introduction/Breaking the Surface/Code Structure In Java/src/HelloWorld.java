// The class, as well as the file, is called HelloWorld.
// A source code file (with the .java extension) typically holds one class definition.
// A class has one or more methods
// The methods must be declared inside the class i.e., within the curly braces of the class

public class HelloWorld {

   // When the JVM starts running, it looks for the main() method and it should look exactly same (but not really)
   public static void main(String[] args) {

      // Within the curly braces of a method, statements are written
   }
}